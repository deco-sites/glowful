import Component from "$store/components/search/FiltersMobile.tsx";
import type { Props } from "$store/components/search/FiltersMobile.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
