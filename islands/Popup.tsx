import Component from "$store/components/ui/Popup.tsx";
import type { Props } from "$store/components/ui/Popup.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
