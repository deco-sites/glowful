import Component from "$store/components/product/ProductImages.tsx";
import type { Props } from "$store/components/product/ProductImages.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
